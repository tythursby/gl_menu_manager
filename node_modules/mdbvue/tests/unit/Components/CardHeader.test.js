import { shallowMount } from '@vue/test-utils'
import mdbCardHeader from '@/components/Components/CardHeader.vue'

describe('Component', () => {
  test('is a Vue instance', () => {
    const wrapper = shallowMount(mdbCardHeader)
    expect(wrapper.isVueInstance()).toBeTruthy()
  })

  test('is slot correct', () => {
    const wrapper = shallowMount(mdbCardHeader, {
      slots: {
        default: 'Test'
      }
    })
    expect(wrapper.text()).toBe('Test')
  })

  test('are classes correct', () => {
    const wrapper = shallowMount(mdbCardHeader, {
      propsData: {
        color: 'primary',
        textColor: 'white'
      }
    })
    expect(wrapper.classes()).toContain('card-header') 
    expect(wrapper.classes()).toContain('primary') 
    expect(wrapper.classes()).toContain('white-text') 
  })
})